import 'package:flutter/material.dart';

class Adverts extends StatelessWidget {
  const Adverts({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text('Adverts');
  }
}
