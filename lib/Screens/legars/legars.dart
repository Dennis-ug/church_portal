import 'package:flutter/material.dart';

class Legars extends StatelessWidget {
  const Legars({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Text('Legars');
  }
}
